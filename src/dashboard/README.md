# Dashboard

TODO: decide framework (React recommended per project plan).

Views needed:
  - Failure category breakdown (chart)
  - Recovered revenue estimate (headline metric)
  - Live decision feed: each entry shows transaction, category,
    recovery probability, action taken, and the LLM-generated explanation
    ("why") -- this auditability is a key selling point, don't cut it.
