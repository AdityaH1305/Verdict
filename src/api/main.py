"""
Backend API (FastAPI).

TODO:
  - Endpoint to ingest/simulate a batch of failed transactions
  - Endpoint to run the RecoveryAgent over a transaction and return the
    decision + explanation
  - Endpoint(s) for the dashboard: failure breakdown stats, recovered
    revenue estimate, live decision feed
"""
