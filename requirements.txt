# Data + ML
pandas
numpy
scikit-learn
xgboost
joblib            # model (de)serialization -- imported directly by src/models/*
matplotlib

# API
fastapi
uvicorn
pydantic

# LLM -- provider-swappable, see src/agent/llm_adapter.py
google-genai      # GeminiAdapter (active path; free tier)
anthropic         # ClaudeAdapter (retained, dormant without a paid key)
python-dotenv

# Dev
pytest

# Deliberately NOT here -- both were listed but never imported:
#   jupyter  -- ~100 MB, and its jupyterlab extension paths are deep enough to
#               trip Windows' 260-char path limit when the project sits in an
#               already-deep directory (this aborts the WHOLE install, not just
#               jupyter). Nothing here imports it; `pip install jupyter`
#               separately if you want notebooks.
#   seaborn  -- matplotlib does all the plotting.
